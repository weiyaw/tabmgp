expdir: ???
batch: null
loss: likelihood
seed: 999

rollout_times: 100
rollout_length: 500
n_estimators: 4
average_before_softmax: False

eval_t: [250, 500, 750, 1000, 2000, 3000, 4000, 5000]
trace: True
resolution: 50

lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  run:
    dir: "./${expdir}"
  sweep:
    dir: ${hydra.run.dir}
    subdir: .
