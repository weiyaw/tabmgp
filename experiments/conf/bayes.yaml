expdir: ???
loss: likelihood
seed: 999
run_name: bayes-${prior}

prior: asymp
step_size: 1e-3
n_warmup: 1000
n_samples: 2000
n_chains: 4

lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  run:
    dir: "./${expdir}"
  output_subdir: null
  job_logging:
    handlers:
      file:
        filename: "./${expdir}/logs/${run_name}.log"
        delay: true
