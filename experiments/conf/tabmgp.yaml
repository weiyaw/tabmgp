expdir: ???
batch: null
loss: likelihood
seed: 999
run_name: tabmgp

rollout_times: 100
forward_steps: [250, 500]
n_estimators: 4
average_before_softmax: False

trace: True
resolution: 50

lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  run:
    dir: "./${expdir}"
  output_subdir: null
  job_logging:
    handlers:
      file:
        filename: "./${expdir}/logs/${run_name}.log"
        delay: true
