expdir: ???
batch: null
loss: likelihood
seed: 999

rollout_times: 100
forward_steps: 2000

eval_t: [250, 500, 750, 1000, 2000, 3000, 4000, 5000]
trace: True
resolution: 50

lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  run:
    dir: "./${expdir}"
  sweep:
    dir: ${hydra.run.dir}
    subdir: .
